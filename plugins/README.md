# Place third-party NOVA Layer plugins here.
#
# Each plugin directory must contain:
#   manifest.json
#   plugin.py (or the entry_module declared in the manifest)
#   resources/   (optional)
#   models/      (optional)
#   README.md    (recommended)
#
# Plugins are discovered at application startup. Failures are isolated and
# never prevent Core from starting. No automatic downloads.
#
# Supported plugin_type values: inference | matting | host_adapter
# Supported sdk_version: 1.0
